name: check
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      #      - uses: actions/setup-node@v3
      #        with:
      #          node-version: 22.12
      - name: install deps
        run: npm ci
      - name: lint
        run: npm run lint
